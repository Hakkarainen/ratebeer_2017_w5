<p id="notice"><%= notice %></p>

<%= form_tag places_path do %>
    city <%= text_field_tag :city, params[:city] %>
    <%= submit_tag "Search" %>
<% end %>

<p> </p>
<p> </p>
<h2>     Beer Places </h2>
<p> </p>

<% if session[:last_city].nil? %>
    <p>no city searched yet</p>
<% else %>
    <p>previous city searched: <%= session[:last_city] %></p>
<% end %>

<% if @places %>
    <table>
      <!--<thead>-->
      <!--<% Place.rendered_fields.each do |f| %>-->
          <!--<td><%=f %></td>-->
      <!--<% end %>-->
      <!--</thead>-->

      <% @places.each do |place| %>
          <tr>
            <% Place.rendered_fields.each do |f| %>

                  <% if f==:name %>
                      <td> <%= link_to place.send(f), place %></td>
                  <% end %>

            <% end %>
          </tr>
      <% end %>
    </table>
<% end %>